# frozen_string_literal: true

source 'https://rubygems.org'

gem 'json'
gem 'rbnacl'
gem 'roda'

group :development do
  gem 'bundler-audit'
  gem 'rubocop', require: false
end

group :test do
  gem 'minitest'
  gem 'rack-test'
end
