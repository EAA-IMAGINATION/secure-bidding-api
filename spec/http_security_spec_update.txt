# Update all mock_routing.headers to mock_routing.env
# Change 'AUTHORIZATION' to 'HTTP_AUTHORIZATION'
# Use the helper method define_method_on_object to set env

# Original pattern:
def mock_routing.headers
  {}
end

# Updated pattern:
def mock_routing.env
  {}
end

# For tests using define_method_on_object with headers parameter, change to:
define_method_on_object(mock_routing, :env, { 'HTTP_AUTHORIZATION' => "Bearer #{token}" })
